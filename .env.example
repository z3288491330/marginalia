# Neon Postgres 连接串。到 https://neon.tech 建一个免费项目，
# 复制 "Connection string"（带 ?sslmode=require 的那条）粘到下面，
# 然后把本文件另存为 .env.local（已被 .gitignore 忽略，不会提交）。
DATABASE_URL="postgresql://user:password@ep-xxxx.region.aws.neon.tech/neondb?sslmode=require"

# 管理后台密码（第 4 步管理后台会用到，现在可先留空占位）。
ADMIN_PASSWORD=""
